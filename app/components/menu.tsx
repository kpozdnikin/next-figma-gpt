import styled from 'styled-components';

export const Menu = styled.div`
    width: 490px;
    height: 20px;
    background-color: rgba(0, 0, 0, 0);
    display: flex;
    flex-wrap: nowrap;
    gap: 24px;
    position: relative;
`;