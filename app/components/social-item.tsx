import styled from 'styled-components';

export const SocialItem = styled.span`
    font-family: 'Gilroy', sans-serif;  // You'd need to import or define the Gilroy font elsewhere in your app
    font-weight: 500;
    font-size: 16px;
    color: #ffffff;  // Solid white color as provided in the JSON
    text-align: left;
    line-height: 20px;
    white-space: nowrap;  // To ensure the text does not wrap
    letter-spacing: 0;
`;